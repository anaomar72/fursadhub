package com.fursadhub.candidacy.domain;

/** Internship offer states — CLAUDE.md section 38. Do not add states without explicit approval. */
public enum OfferStatus {
    PENDING,
    ACCEPTED,
    DECLINED,
    EXPIRED,
    WITHDRAWN
}
