package com.fursadhub.internshipmanagement.domain;

/** Frozen final-report states (CLAUDE.md section 45). */
public enum FinalReportState {
    DRAFT,
    SUBMITTED,
    NEEDS_REVISION,
    APPROVED
}
