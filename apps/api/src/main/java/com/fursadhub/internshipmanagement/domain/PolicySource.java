package com.fursadhub.internshipmanagement.domain;

/** Which level of configuration produced a resolved policy. Explainability only, never logic. */
public enum PolicySource {
    DEPARTMENT,
    UNIVERSITY,
    PLATFORM_DEFAULT
}
