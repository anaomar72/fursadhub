package com.fursadhub.opportunity.domain;

/** Internship opportunity states — CLAUDE.md section 33. Do not add states without explicit approval. */
public enum OpportunityStatus {
    DRAFT,
    PUBLISHED,
    PAUSED,
    CLOSED,
    CANCELLED
}
