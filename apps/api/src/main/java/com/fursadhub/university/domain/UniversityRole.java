package com.fursadhub.university.domain;

/** University staff roles — CLAUDE.md section 23. Do not add roles without explicit approval. */
public enum UniversityRole {
    UNIVERSITY_ADMIN,
    DEPARTMENT_COORDINATOR,
    UNIVERSITY_SUPERVISOR
}
