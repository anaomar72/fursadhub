import { useId, type ChangeEvent } from 'react'
import { cn } from '../../lib/utils/cn'
import { Icon } from './Icon'
export interface FileUploadProps { label:string; hint?:string; accept?:string; multiple?:boolean; disabled?:boolean; invalid?:boolean; onFiles:(files:File[])=>void; className?:string }
export function FileUpload({label,hint,accept,multiple,disabled,invalid,onFiles,className}:FileUploadProps){const id=useId();const change=(e:ChangeEvent<HTMLInputElement>)=>onFiles(Array.from(e.target.files??[]));return <label htmlFor={id} className={cn('flex min-h-32 cursor-pointer flex-col items-center justify-center rounded-lg border border-dashed bg-surface p-5 text-center transition-colors hover:bg-surface-muted focus-within:ring-2 focus-within:ring-focus-ring',invalid?'border-danger':'border-border-strong',disabled&&'pointer-events-none opacity-50',className)}><Icon name="upload" className="size-7 text-brand-primary"/><span className="mt-2 text-sm font-semibold text-foreground">{label}</span>{hint&&<span className="mt-1 text-xs text-muted">{hint}</span>}<input id={id} className="sr-only" type="file" accept={accept} multiple={multiple} disabled={disabled} aria-invalid={invalid||undefined} onChange={change}/></label>}
