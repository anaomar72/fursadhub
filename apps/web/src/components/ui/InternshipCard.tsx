import type { ReactNode } from 'react'
import { Card } from './Card'
import { Badge } from './Badge'
export interface InternshipCardProps { title:string; organization:string; location?:string; workMode?:string; duration?:string; deadline?:ReactNode; logo?:ReactNode; actions?:ReactNode; children?:ReactNode; className?:string }
export function InternshipCard({title,organization,location,workMode,duration,deadline,logo,actions,children,className}:InternshipCardProps){return <Card interactive className={className}><div className="flex items-start gap-3">{logo&&<div className="shrink-0">{logo}</div>}<div className="min-w-0 flex-1"><h3 className="line-clamp-2 font-semibold text-foreground">{title}</h3><p className="mt-1 truncate text-sm text-foreground-secondary">{organization}</p></div></div><div className="mt-3 flex flex-wrap gap-2">{location&&<Badge>{location}</Badge>}{workMode&&<Badge tone="brand">{workMode}</Badge>}{duration&&<Badge>{duration}</Badge>}</div>{children&&<div className="mt-3 text-sm text-foreground-secondary">{children}</div>}<div className="mt-4 flex flex-wrap items-center justify-between gap-3">{deadline&&<div className="text-xs text-muted">{deadline}</div>}{actions}</div></Card>}
