import { cn } from '../../lib/utils/cn'
export function ProgressIndicator({value,max=100,label,showValue=true,className}:{value:number;max?:number;label:string;showValue?:boolean;className?:string}){const percent=Math.min(100,Math.max(0,(value/max)*100));return <div className={className}><div className="mb-1.5 flex justify-between gap-3 text-sm"><span className="font-medium text-foreground">{label}</span>{showValue&&<span className="text-muted">{Math.round(percent)}%</span>}</div><div className="h-2 overflow-hidden rounded-full bg-surface-muted" role="progressbar" aria-label={label} aria-valuemin={0} aria-valuemax={max} aria-valuenow={value}><div className={cn('h-full rounded-full bg-brand-primary transition-[width] duration-300 motion-reduce:transition-none')} style={{width:`${percent}%`}}/></div></div>}
